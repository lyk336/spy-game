export interface IGame {
  spyId: string;
  locationName: string;
  locationTheme: string;
  isGameEnded: boolean;
  // --------------------
  questionNumber: number;
  isRoundEnd: boolean;
  // round: number;
}
